import { StyleSheet } from "react-native"

export const colors = {
    white : '#FFF',
    primary: '#050a30',
    botpri: '#5cb6f9',
    botsen: '#cae8ff'
}

export const styleContainer = StyleSheet.create({
    container: {
        flex: 1
    }
})